{
  "version": 2,
  "buildCommand": "python3 -m pip install --upgrade pip && pip install -r requirements.txt && python3 -m mkdocs build",
  "outputDirectory": "site",
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "site",
        "zeroConfig": true
      }
    }
  ],
  "framework": null
} 